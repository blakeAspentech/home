class Profile
{
	// variables
	//string name
	//string profileId
	//AuthProfile auth
	//Set<BlobMetaData> blobs
	//Theme? userTheme
	//bool isAutosortingBlobs
	//Icon? icon

	
	// methods
	//constructor(new)
	//constructor(string)
	//Set<BlobMetaData> getAllBlobs()
	//bool resetPromptsForBlob(BlobId)
	//bool resetAllPrompts()
	//bool resetAllWarnings()
	
	//bool addBlob(BlobMetaData)
	//bool removeBlob(BlobId)
	//bool removeAllBlobs()

	// the following may be moved to AuthProfile?
	//bool login()
	//bool logout()

	// the following may be moved permanently to ProfileManager
	//string exportProfile()
	//bool deleteProfile()


	/*
	Profile(string)
	{
		profileJSON = Json.parse(string)
		name =
	}

	*/
}