class AuthProfile
{
	// variables -- ALL must be SECURE
	//string password
	//bool isAdmin
	//Timespan? timeout // if none, no timeout set
	//bool? isCollaborativeUser // default false, beta feature. collaborative blobs will be primary
	//

	

	// methods
	//constructor(parentProfileId)
	//bool login(string password?) // figure out how to do this secure
	//bool logout()
	
	
	//bool setTimeout(Timespan)
	//bool setIsCollaborative(bool)
	//string exportProfileWithAuth() // dangerous
	//bool importProfileWithAuth() // dangerous
	//bool deleteProfile()

}